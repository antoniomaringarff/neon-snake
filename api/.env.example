# API Configuration
NODE_ENV=production
PORT=3003

# Database Configuration
DB_HOST=db
DB_PORT=5432
DB_NAME=viborita
DB_USER=postgres
DB_PASSWORD=your_secure_password_here

# JWT Secret (IMPORTANT: Change this to a strong random string in production!)
JWT_SECRET=your_jwt_secret_here_change_this

# CORS Origin (URL where the frontend will be hosted)
CORS_ORIGIN=https://neon.snake.sally.ar
